/**
 * Jobs Queries - Index
 */
const jobs = require('./jobs.queries');
const workflow = require('./workflow.queries');
const team = require('./team.queries');
const equipment = require('./equipment.queries');
const purchasing = require('./purchasing.queries');
const inspection = require('./inspection.queries');
const material = require('./material.queries');

module.exports = { jobs, workflow, team, equipment, purchasing, inspection, material };
